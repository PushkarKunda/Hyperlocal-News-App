// import Constants from 'expo-constants';
// import { createClient, SupabaseClient } from '@supabase/supabase-js';

// const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL ?? Constants.manifest?.extra?.supabaseUrl;
// const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY ?? Constants.manifest?.extra?.supabaseAnonKey;

// if (!supabaseUrl || !supabaseAnonKey) {
//   // We'll still export a client but developers should set env vars for real usage
//   console.warn('Supabase client not fully configured. Set EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY in .env');
// }

// export const supabase: SupabaseClient = createClient(supabaseUrl ?? '', supabaseAnonKey ?? '');
